# ========================================================
# Skill Swap Platform - Production Environment Template
# ========================================================

# Server Configuration
PORT=5000
NODE_ENV=production
JWT_SECRET=super-secure-production-jwt-secret-key-2026
CORS_ORIGIN=*

# Rate Limiting
RATE_LIMIT_MAX=1000
AUTH_RATE_LIMIT_MAX=150

# Database Configuration (Optional PostgreSQL)
# DB_DIALECT=postgres
# DATABASE_URL=postgres://user:password@localhost:5432/skillswap

# Client Configuration (Optional if frontend deployed separately)
# VITE_API_URL=https://api.yourdomain.com
